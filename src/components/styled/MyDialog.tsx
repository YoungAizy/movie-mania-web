import { Dialog } from "@mui/material";
import { styled } from "@mui/system";

const MyDialog = styled(Dialog)({

        '.MuiDialog-paper':{
            borderRadius: 8,
            backgroundColor: "#677481f0",
            width: "80%",
            padding: "16px 8px"
        }

})

export default MyDialog;